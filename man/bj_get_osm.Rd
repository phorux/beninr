% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osm.R
\name{bj_get_osm}
\alias{bj_get_osm}
\title{Download Benin OpenStreetMap Data from Geofabrik}
\usage{
bj_get_osm(
  format = c("shp", "gpkg", "pbf"),
  dest_dir = ".",
  layer = NULL,
  list_layers = FALSE,
  overwrite = FALSE
)
}
\arguments{
\item{format}{\code{character}. File format to download. One of:
\itemize{
\item \code{"shp"} — ESRI Shapefile (\code{.shp.zip}). Extracts to multiple
theme-based shapefiles (roads, buildings, waterways, etc.).
\item \code{"gpkg"} — GeoPackage (\code{.gpkg.zip}). Same content as
Shapefile but in modern OGC GeoPackage format.
\item \code{"pbf"} — Protocolbuffer Binary Format (\code{.osm.pbf}). Compact
format suitable for Osmium, Osmosis, osm2pgsql.
}}

\item{dest_dir}{\code{character}. Directory where downloaded files will be
saved. Defaults to \code{"."} (current working directory). Created
automatically if it does not exist.}

\item{layer}{\code{character}. For \code{"shp"} and \code{"gpkg"} formats, the name
of the layer to read after extraction (e.g. \code{"gis_osm_roads_free_1"}).
If \code{NULL} (default), the archive is extracted but no \link[sf:sf]{sf} object
is returned. Use \code{bj_get_osm(format, dest_dir, list_layers = TRUE)} to
see all available layers.}

\item{list_layers}{\code{logical}. If \code{TRUE}, lists all available layers in
the extracted archive and returns them as a character vector without
reading any layer. Defaults to \code{FALSE}.}

\item{overwrite}{\code{logical}. If \code{TRUE}, re-downloads the file even if it
already exists locally. Defaults to \code{FALSE}.}
}
\value{
\itemize{
\item If \code{list_layers = TRUE}: a \code{character} vector of available layer names.
\item If \code{layer = NULL}: invisibly returns the path to the downloaded or
extracted file(s).
\item If \code{layer} is specified: an \link[sf:sf]{sf} data frame of the requested
layer.
}
}
\description{
Downloads the latest OpenStreetMap data for Benin from the
\href{https://download.geofabrik.de/africa/benin.html}{Geofabrik download server}.
}
\examples{
\dontrun{
# Download shapefile archive (extract only)
bj_get_osm(format = "shp", dest_dir = "data/osm")

# List available layers after download
bj_get_osm(format = "shp", dest_dir = "data/osm", list_layers = TRUE)

# Download and read a specific layer
roads <- bj_get_osm(
  format   = "shp",
  dest_dir = "data/osm",
  layer    = "gis_osm_roads_free_1"
)

# GeoPackage format
poi <- bj_get_osm(
  format   = "gpkg",
  dest_dir = "data/osm",
  layer    = "gis_osm_pois_free_1"
)

# PBF format (use with osmextract)
pbf_path <- bj_get_osm(format = "pbf", dest_dir = "data/osm")
roads <- osmextract::oe_read(pbf_path, layer = "lines")

# Force re-download
bj_get_osm(format = "shp", dest_dir = "data/osm", overwrite = TRUE)
}

}
\seealso{
\itemize{
\item \code{\link[=bj_get_arcgis_layer]{bj_get_arcgis_layer()}} for ArcGIS Online data
}
}
